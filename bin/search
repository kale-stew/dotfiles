#!/bin/sh
#
# Quick search in a directory for a string ($@).
# Uses ripgrep (rg) — respects .gitignore by default.

set -e

if command -v rg >/dev/null 2>&1; then
    rg --smart-case "$@"
else
    echo "ripgrep not found. Install: brew install ripgrep" >&2
    exit 1
fi
